// Customs component

function Page() {
  return (
    <ol>
      <li>It is mostly used</li>
      <li>It is mostly used</li>
      <li>It is mostly used</li>
      <li>It is mostly used</li>
      <li>It is mostly used</li>
    </ol>
  );
}

// ReactDOM.render(Page(), document.getElementById("root"));
ReactDOM.render(<Page />, document.getElementById("root"));
console.log(444);
